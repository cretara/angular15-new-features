import { MouseEnterDirective } from "./mouse-enter.directive";

describe("MousenterDirective", () => {
  it("should create an instance", () => {
    const directive = new MouseEnterDirective();
    expect(directive).toBeTruthy();
  });
});
